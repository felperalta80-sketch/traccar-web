# Variables de entorno de la app (Vite).
# Copiá este archivo a .env y ajustá los valores si hace falta.

# Versión de la app expuesta al frontend. Por defecto toma la versión
# declarada en package.json (no hace falta cambiarla manualmente).
VITE_APP_VERSION=$npm_package_version
